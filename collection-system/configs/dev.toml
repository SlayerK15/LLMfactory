# Dev overrides — lower limits for faster iteration
[app]
log_level = "DEBUG"

[query_agent]
max_depth = 2
max_queries = 50

[search.searxng]
base_url = "http://localhost:8888"

[scraper]
concurrency = 5
per_url_timeout_s = 15

[reliability]
global_timeout_s = 300
